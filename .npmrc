# Hoist fdir / tinyglobby so `import "fdir"` from tinyglobby always resolves (fixes broken
# symlinks after interrupted installs and odd resolution on some Node versions).
public-hoist-pattern[]=*fdir*
public-hoist-pattern[]=*tinyglobby*
